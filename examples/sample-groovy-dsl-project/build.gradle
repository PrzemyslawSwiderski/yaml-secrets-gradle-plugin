plugins {
    id "com.pswidersk.yaml-secrets-plugin" version "1.0.5"
}

Long testProp2 = secrets.getValue("test.testProp2")
assert testProp2 == 3
Map testMap = secrets.getValue("test")
assert testMap == [testProp1: "test", testProp2: 3, testProp3: [nested1: 2, nested2: "test", nested3: [2, 3, 2]]]
